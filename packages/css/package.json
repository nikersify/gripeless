{
  "name": "@gripeless/css",
  "private": true,
  "version": "0.0.0",
  "main": "dist/index.css",
  "scripts": {
    "build:single": "postcss source.css --output dist/index.css --verbose",
    "build": "NODE_ENV=production yarn build:single",
    "dev": "yarn build:single --watch"
  },
  "devDependencies": {
    "@fullhuman/postcss-purgecss": "^2.0.5",
    "@types/cssnano": "^4.0.0",
    "cssnano": "^4.1.10",
    "postcss-cli": "^7.1.0",
    "postcss-each": "^0.10.0",
    "tailwindcss": "^1.1.4"
  }
}
